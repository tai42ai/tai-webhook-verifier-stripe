name: Feature request
description: Propose an addition or change to tai42-webhook-verifier-stripe
labels: ["enhancement"]
body:
  - type: textarea
    id: description
    attributes:
      label: Description
      description: What should be added or changed, and what problem does it solve? Remember the plugin rule — tai42-webhook-verifier-stripe depends on tai42-contract only and never imports the skeleton.
    validations:
      required: true
  - type: input
    id: version
    attributes:
      label: tai42-webhook-verifier-stripe version
      description: The version you are using, if relevant.
      placeholder: "0.1.2"
    validations:
      required: false
